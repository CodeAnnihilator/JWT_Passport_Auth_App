import path from 'path'

export const ROOT = path.resolve(__dirname, '../')
export const SRC = path.resolve(ROOT, 'src')
export const STATIC = path.resolve(ROOT, 'dist')
export const ASSETS = path.resolve(SRC, 'assets')
export const STYLES = path.resolve(SRC, 'styles')
